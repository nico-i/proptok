iOS_standalone_indexeddb_eviction: takes may be cleared after inactivity in iOS PWA. ACCEPTED risk for MVP. mitigation (export/offload) deferred.
iOS_autoplay: programmatic play() may reject outside gesture. PlaybackView falls back to tap-to-play overlay. do NOT assume autoplay works.
https_required: getUserMedia needs secure context. on-device test via `npm run dev:https` (vite --https) or tunnel. localhost exempt.
video_elements: MUST be playsinline + preview muted, else iOS forces native fullscreen player / audio feedback.
save_icon_semantics: IndexedDB persistence is automatic+silent on stop (backup). The ActionBar Save icon now EXPORTS the take to device storage via Web Share API (lib/share.ts) on tap, then animates idle->checkmark only on success ("shared"). Requires a user gesture (why it's on tap, not auto). Cancelled/unsupported/failed => no checkmark.
device_save: lib/share.ts uses navigator.share({files:[File]}) + canShare guard. File name proptok-<iso>.<ext>, ext derived from blob.type (mp4/webm). No API silently writes to camera roll from a PWA; the OS share sheet handles the actual save. File System Access API rejected (desktop-only, unsupported on iOS/Android).
esbuild_postinstall: npm blocked esbuild install script (project-scoped, --allow-scripts disallowed). binary still present & working; if vite fails to start check node_modules/esbuild/bin/esbuild.
tdd_waiver: TDD-first EXPLICITLY WAIVED for this MVP slice (user decision, recorded). tests written together next session. code kept testable (pure machine, isolated IO). NOT a silent drop of AGENTS.md DoD.
parody_legal: TikTok trade dress parody is user-owned risk decision. GOAL = imitate/evoke TikTok obviously (parody) but NEVER match colors/fonts/assets one-to-one. Overlay uses TikTok-style layout (For You tabs, like/comment/share rail, @handle). Keep the Parody badge.
color_config: ALL colors live in src/tokens.css (single configurable source). Palette deliberately imitates TikTok but offset from real hexes: cyan #25F4EE->#32E0D6, red #FE2C55->#F5385F, black #000->#0A0A0D. Never hardcode a color in a component; use var(--pt-*). Font = generic grotesque stack (Helvetica Neue/Arial/system), not TikTok's proprietary type.
